version: "3.8"

services:
  app:
    container_name: maset
    image: maset:latest
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: development
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules